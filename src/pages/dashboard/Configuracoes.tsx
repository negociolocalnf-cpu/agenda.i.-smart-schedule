import { Settings } from "lucide-react";
import PlaceholderPage from "./PlaceholderPage";

const Configuracoes = () => (
  <PlaceholderPage
    title="Configurações"
    subtitle="Preferências da clínica e da conta"
    icon={Settings}
    description="Em breve: dados da clínica, integrações (WhatsApp, e-mail), permissões de usuários e plano."
  />
);

export default Configuracoes;
