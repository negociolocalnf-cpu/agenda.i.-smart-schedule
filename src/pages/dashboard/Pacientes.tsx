import { Users } from "lucide-react";
import PlaceholderPage from "./PlaceholderPage";

const Pacientes = () => (
  <PlaceholderPage
    title="Pacientes"
    subtitle="Cadastro e histórico de pacientes"
    icon={Users}
    description="Em breve: cadastro completo, prontuário, anamnese e histórico de procedimentos por paciente."
  />
);

export default Pacientes;
